## What & why

<!-- What does this PR change, and which spec section(s) does it implement?
     Link the relevant part of docs/specs/. -->

## Checklist

- [ ] Change is scoped to a single concern
- [ ] Relevant spec section referenced (`docs/specs/…`)
- [ ] `test/` conformance harness passes (`cd test && go test ./...`)
- [ ] GitHub Actions `uses:` are pinned to full commit SHAs (no floating tags)
- [ ] Commits are DCO signed-off (`git commit -s`) and signed
